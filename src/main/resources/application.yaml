server:
  port: 8080
spring:
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: "my-awesome-app"
  mail:
    host: smtp.gmail.com  # Protocol
    port: 587             # Default port
    username: supteam2468         # Email ไม่ต้องใส่ domain
    password: cbcnsqynczzoitcf    # App password ที่ได้จาก gmail function verify 2 step
    properties:
      mail:
        smtp:
          auth: true  # Authorization มีการเข้ารหัสไหม มี ssl หรือไม่
          starttls:
            enable: true
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/backend
    username: postgres
    password: syspass
  servlet:
    multipart:
      enabled: true
      max-file-size: 2MB    # Size file upload
      max-request-size: 10MB  # Total size all time upload
---
app:
  token:
    secret: m#MySecretGGWP2021
    issuer: BackendService
  email:
    from: supteam2468@gmail.com # Email ต้นทางที่ส่ง


